import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '01_counter_app-standalone',
  webDir: 'www'
};

export default config;
